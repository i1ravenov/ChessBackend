namespace SharedTests;

public class Program
{
    static async Task Main()
    {
        var engine = new 
        //
        // // Path to the Stockfish binary (relative to project directory)
        // engine.LaunchEngine("engines/stockfish");
        //
        // // Wait until engine is ready
        // bool ready = await engine.IsReady();
        // Console.WriteLine($"Engine ready: {ready}");
        //
        // // Set up starting position
        // await engine.SetPosition("startpos");
        //
        // // Ask for best move
        // string bestMove = await engine.GetBestMoveAsync();
        // Console.WriteLine($"Best move from starting position: {bestMove}");
        //
        // // Stop engine
        // engine.KillEngine();
    }
}